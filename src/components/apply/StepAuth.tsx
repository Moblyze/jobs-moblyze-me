'use client';

/**
 * StepAuth — phone/OTP authentication step in the apply wizard.
 * Full implementation in 04-05 plan.
 */
export function StepAuth() {
  return (
    <div className="text-center py-8">
      <p className="text-muted-foreground">Authentication step coming soon.</p>
    </div>
  );
}
