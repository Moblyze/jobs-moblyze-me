'use client';

/**
 * StepRoles — role/trade selection step in the apply wizard.
 * Full implementation in 04-05 plan.
 */
export function StepRoles() {
  return (
    <div className="text-center py-8">
      <p className="text-muted-foreground">Role selection step coming soon.</p>
    </div>
  );
}
